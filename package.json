{
  "scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "start": "node server/index.js",  // Start the server
  "server": "nodemon server/index.js",
  "client": "npm start --prefix client",  // Start the React client
  "dev": "concurrently \"npm run server\" \"npm run client\"",  // Start both server and client concurrently
  "data:import": "node server/seeder",
  "data:destroy": "node server/seeder -d",
  "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client"
},
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "colors": "^1.4.0",
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^8.5.1",
    "moment": "^2.29.4",
    "mongoose": "^6.6.1",
    "concurrently": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
